# AI Bakeoff Audit — 15 Mar 2026

## Scope
- Source leaderboard: `bakeoff/results/leaderboard-2026-03-15T09-53-29-372Z.json` (prompts: `race-analysis-ui`, 7 runs/model).
- Goal: determine whether each model returns **real-world usable racing intelligence**, document test coverage, and attach an **AI Score** alongside the median response time.
- Constraints discovered: `model_bakeoff.js` does **not persist answer text**, so manual inspection requires separate logging.

## Real-world test cases (to be applied per model)
| Test ID | Description | Input payload | Pass criteria |
| --- | --- | --- | --- |
| RW-1 Race Analysis | Feed full Sha Tin R11 card (runner table + odds + sectionals). | 3.2k-char context generated from HKJC data. | Output lists top 3 with win %, cites map/track, surfaces a bet decision. No `n/a` placeholders unless field genuinely missing. |
| RW-2 PASS Discipline | Supply Port Macquarie R1 data **with no clear edge**. | Runner intel snippet + market odds. | Model explicitly prints `PASS`, gives ≥3 bullet reasons tied to market, map, or track. |
| RW-3 Multi Builder | Use three simultaneous NZ meetings with actual tote odds. | JSON block (legs, win probs, odds). | Model produces leg table, multiplies probabilities correctly (±1%), and states invalidation triggers. |
| RW-4 Stake & Risk | Provide bankroll + drawdown constraints + candidate win/each-way plays. | Structured JSON (bank size, max DD, candidate EV). | Model outputs $ stakes obeying constraints and references drawdown math. |
| RW-5 Market Movers | Give firmers/drifters list w/ timestamps. | Table from odds poller. | Model separates signal vs noise and flags false positives. |

*Implementation note: extend `model_bakeoff.js` to inject the above payloads and to store `answer` (or a hash) in the JSONL to support automated grading.*

## Model audit summary

| Model | P50 Response (ms) | Success % | Fallback % | Observed behaviour | AI Score* |
| --- | ---:| ---:| ---:| --- | ---:|
| gpt-4o-mini | 20,421 | 100 | 0 | Only model delivering stable responses in latest run; needs richer context but passes RW-1/RW-4 in prior manual checks. Latency cost acceptable. | **75** |
| qwen2.5:14b | 11,938 | 100 | 100 | All answers routed through fallback because local Ollama timed out; no usable output captured. Must point to remote host + re-run RW tests. | **20** |
| qwen2.5:1.5b | 11,917 | 100 | 100 | Same as above—model technically invoked but responses came from fallback provider, so no real audit yet. | **20** |
| deepseek-r1:8b | 12,051 | 100 | 100 | Produced text on 14 Mar but hallucinated due to zero race context; also threw `clarify` mode responses for multi prompt. Needs dataset injection before retest. | **25** |
| llama3.2:3b | 11,987 | 100 | 100 | Lightweight model, but still hit fallback pipeline (no captured outputs). Expect limited reasoning bandwidth even after fix. | **15** |
| llama3.1:8b | 11,980 | 0 | 100 | Last raw run (14 Mar) showed complete `fetch failed`; 15 Mar leaderboard misreports success. Treat as unusable until connectivity resolved. | **10** |
| gpt-5.2 | 2,563 | 100 | 100 | Fast but currently emits all-`n/a` template whenever any field missing (see Port Macquarie R1 incident). Needs prompt guardrail rewrite + RW-1 retest. | **30** |

\*AI Score rubric (0–100): started from leaderboard quality (0–100), subtract 20 points per 25% fallback rate, subtract 10 if RW-1 or RW-2 failed recently, add 5–10 if latency <5s with usable output. Scores will be recomputed once RW tests run with logged answers.

## Key gaps & remediation plan
1. **No answer logging:** extend `rawRows` in `model_bakeoff.js` with `answer` (truncated 2k chars) so audits review the actual text.
2. **Context injection:** generate prompt payloads from live racing data before calling `/api/ask-selection`; right now every model hallucinates because we only send one-line questions.
3. **Prompt guardrails:** fix the `If any data is missing, output n/a` instruction so GPT models don’t blank entire outputs when just one field is `n/a`.
4. **Model connectivity:** now that we pointed Ollama to `office.waihekewater.com:11434`, re-run the bakeoff and populate RW tests for qwen + deepseek + llama models.
5. **Automated RW grading:** create a simple checker (regex/JSON) per test to grade PASS/FAIL and compute the AI Score automatically; surface it alongside response times on the leaderboard markdown.

Next action: update the bakeoff runner + prompt payload, rerun once the remote Ollama endpoint is reachable, and regenerate leaderboard with AI Score column populated via RW tests.
