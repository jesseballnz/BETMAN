# brave_search

Use Brave Search to search the public web and return the top results.

## Inputs
- query (string): search query

## Tool
Run:
~/.openclaw/workspace/tools/brave_search.sh "<query>"

## Output
Return the results exactly as printed by the tool (title, URL, snippet).
